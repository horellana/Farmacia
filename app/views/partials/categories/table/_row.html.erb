<tr>

  <td>
    $ <%= category.description %>
  </td>

  <% if current_user.admin? %>
    <td>
      <%= link_to 'Editar', edit_category_path(category) %>
    </td>
  <% end %>

  <% if current_user.admin? %>
    <td><%= link_to 'Eliminar', category_path(category),
	method: :delete,
	data: { confirm: 'Estas Seguro?' } %>
    </td>
  <% end %>

</tr>
