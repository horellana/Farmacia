<% if model.errors.any? %>
  <div class="alert alert-danger">
    <%= pluralize(model.errors.count, "errors") %>

    <ul>
      <% model.errors.full_messages.each do |message| %>
	<li>
	  <%= message %>
	</li>
      <% end %>
    </ul>
  </div>
<% end %>
